// Expo config plugin entry (plan §3.5) — loaded ONLY by Expo's config system
// when the consumer lists "@galva/react-native" in app.json "plugins".
// Bare React Native never reads this file.
module.exports = require('./plugin/build');
