// Expo config plugin entry. Loaded only by Expo's config system when a managed
// app lists "@galva/react-native" in app.json "plugins". Bare RN never reads it.
module.exports = require('./plugin/build');
