# Generated by `expo prebuild` (regenerated by the E2E scripts/CI).
/ios/
/android/

# Local tarball of the SDK, packed by the E2E script. `npm pack` is not
# byte-reproducible, so its integrity differs every run — never commit a lockfile
# that would pin it (the E2E installs with --no-package-lock).
galva.tgz
package-lock.json

# Installed deps + Expo/Metro caches.
node_modules/
.expo/
*.log
